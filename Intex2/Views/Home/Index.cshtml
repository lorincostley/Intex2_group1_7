@{
    ViewData["Title"] = "Home Page";
}

@{
    ViewData["Title"] = "Product List";
}

@model ProductListViewModel

<div class="container">
    <div class="row">
        <!-- Top Rated Products Section -->
        <div class="col-lg-12">
            <section class="py-5">
                <div class="container px-4 px-lg-5 mt-5">
                    <h3>Top Rated Products</h3>
                    <div class="row gx-4 gx-lg-5 row-cols-1 row-cols-md-3 row-cols-xl-3 justify-content-center">
                        <!-- Display top-rated products here -->
                       @*  @foreach (Product p in Model.TopRatedProducts.Take(3))
                        {
                            <div class="col mb-5">
                                <a href="@Url.Action("ProductDetails", "Home", new { ProductId = p.ProductId })" class="card-link">
                                    <div class="card h-100 border-0">
                                        <partial name="ProductSummary" model="p" />
                                    </div>
                                </a>
                            </div>
                        } *@
                    </div>
                </div>
            </section>
        </div>
    </div>

    <div class="row">
        <!-- Recommended Products Section -->
        <div class="col-lg-12">
            <section class="py-5">
                <div class="container px-4 px-lg-5 mt-5">
                    <h3>Recommended Products For You</h3>
                    <div class="row gx-4 gx-lg-5 row-cols-1 row-cols-md-3 row-cols-xl-3 justify-content-center">
                        <!-- Display recommended products here -->
                        @* @foreach (Product p in Model.RecommendedProducts.Take(3))
                        {
                            <div class="col mb-5">
                                <a href="@Url.Action("ProductDetails", "Home", new { ProductId = p.ProductId })" class="card-link">
                                    <div class="card h-100 border-0">
                                        <partial name="ProductSummary" model="p" />
                                    </div>
                                </a>
                            </div>
                        } *@
                    </div>
                </div>
            </section>
        </div>
    </div>
</div>

